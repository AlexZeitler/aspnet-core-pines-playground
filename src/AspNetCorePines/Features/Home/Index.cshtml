@{
  Layout = "~/Layouts/_Layout.cshtml";
  ViewData["Title"] = "Home Page";
}

<div class="text-center">
  <h1 class="display-4">Welcome</h1>
  <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
<div class="">
  <div alpine-data="new { Open = false }">
    <button class="p-2 bg-gray-50 border border-gray-400 text-gray-600 rounded-lg text-sm"
            x-on:click="open = !open">Show</button>
    <div x-cloak
         x-show="open">Some details...</div>
  </div>

</div>

<div class="relative w-full mx-auto overflow-hidden text-sm font-normal bg-white border border-gray-200 divide-y divide-gray-200 rounded-md"
     x-data="{
activeAccordion: '',
setActiveAccordion(id) {
this.activeAccordion = (this.activeAccordion == id) ? '' : id
}
}">
  <div class="cursor-pointer group"
       x-data="{ id: $id('accordion') }">
    <button @@click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 text-left select-none group-hover:underline">
      <span>What is Pines?</span>
      <svg :class="{ 'rotate-180': activeAccordion==id }"
           class="w-4 h-4 duration-200 ease-out"
           fill="none"
           stroke="currentColor"
           stroke-linecap="round"
           stroke-linejoin="round"
           stroke-width="2"
           viewBox="0 0 24 24"
           xmlns="http://www.w3.org/2000/svg">
        <polyline points="6 9 12 15 18 9"></polyline>
      </svg>
    </button>
    <div x-cloak
         x-collapse
         x-show="activeAccordion==id">
      <div class="p-4 pt-0 opacity-70">
        Pines is a UI library built for AlpineJS and TailwindCSS.
      </div>
    </div>
  </div>
  <div class="cursor-pointer group"
       x-data="{ id: $id('accordion') }">
    <button @@click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 text-left select-none group-hover:underline">
      <span>How do I install Pines?</span>
      <svg :class="{ 'rotate-180': activeAccordion==id }"
           class="w-4 h-4 duration-200 ease-out"
           fill="none"
           stroke="currentColor"
           stroke-linecap="round"
           stroke-linejoin="round"
           stroke-width="2"
           viewBox="0 0 24 24"
           xmlns="http://www.w3.org/2000/svg">
        <polyline points="6 9 12 15 18 9"></polyline>
      </svg>
    </button>
    <div x-cloak
         x-collapse
         x-show="activeAccordion==id">
      <div class="p-4 pt-0 opacity-70">
        Add AlpineJS and TailwindCSS to your page and then copy and paste any of these elements into your project.
      </div>
    </div>
  </div>
  <div class="cursor-pointer group"
       x-data="{ id: $id('accordion') }">
    <button @@click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 text-left select-none group-hover:underline">
      <span>Can I use Pines with other libraries or frameworks?</span>
      <svg :class="{ 'rotate-180': activeAccordion==id }"
           class="w-4 h-4 duration-200 ease-out"
           fill="none"
           stroke="currentColor"
           stroke-linecap="round"
           stroke-linejoin="round"
           stroke-width="2"
           viewBox="0 0 24 24"
           xmlns="http://www.w3.org/2000/svg">
        <polyline points="6 9 12 15 18 9"></polyline>
      </svg>
    </button>
    <div x-cloak
         x-collapse
         x-show="activeAccordion==id">
      <div class="p-4 pt-0 opacity-70">
        Absolutely! Pines works with any other library or framework. Pines works especially well with the TALL stack.
      </div>
    </div>
  </div>
</div>
